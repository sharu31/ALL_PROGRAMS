#include<stdio.h>
int main()
{
    int n, i=0, bin=0;
    printf("Enter the decimal number: ");
    scanf("%d",&n);
    while(n!=0)
    {
        bin+=(n%2)*pow(10,i);
        n=n/2;
        i++;
    }
    printf("Binary: %d",bin);
    return 0;

}
