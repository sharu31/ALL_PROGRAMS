#include <stdio.h>
#include <math.h>
int main()
{
    int n,sum=0,i=1,m=1;
    printf("Enter the number(n):");
    scanf("%d",&n);
    while(m<=n)
    {
        sum=sum+(pow(i,2));
        i=i+2;
        m++;
    }
    printf("Sum=%d",sum);
    return 0;

}
