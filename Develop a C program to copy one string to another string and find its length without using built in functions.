#include <stdio.h>
int main()
{
    char a[20],b[20];
    int i=0;
    printf("Enter the string:");
    scanf("%s",&a);
    while(a[i]!='\0')
    {
        b[i]=a[i];
        i++;
    }
    b[i]='\0';
    printf("Copied String=%s",b);
    printf("\nLength of the string=%d",i);
    return 0;
}
