#include <stdio.h>
#include<math.h>
float area(float);
int main( )
{
   float radius,A;
   printf("Enter the radius of circle :");
   scanf("%f", &radius);
   A=area(radius);
   printf("Area of circle : %f",A);
   return 0;
}
float area(float r)
{
   float a;
   a=(3.14*pow(r,2));
   return a;
}
